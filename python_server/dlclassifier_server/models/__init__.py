"""Custom model implementations for the DL pixel classifier.

Models in this package are hand-rolled PyTorch modules that are not
available through segmentation_models_pytorch. For SMP-backed architectures
(UNet, UNet++, DeepLab, etc.) see ``services.training_service._create_model``.
"""
